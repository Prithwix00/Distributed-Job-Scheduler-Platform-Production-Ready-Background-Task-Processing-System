# Core runtime deps. These install cleanly on any OS with just Python, no
# system libraries or compilers needed, so the local (SQLite) run works
# everywhere. The PostgreSQL driver lives in requirements-postgres.txt and is
# only needed for the Docker/Postgres deployment.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
croniter==5.0.1
httpx==0.28.1
tenacity==9.0.0
email-validator==2.2.0

# test / dev
pytest==8.3.4
pytest-asyncio==0.25.0
